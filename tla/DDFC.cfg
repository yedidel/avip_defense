\* TLC model configuration for DDFC.tla
\* Run with: tlc -config DDFC.cfg DDFC.tla
\* Expected: all invariants hold; no deadlock at the bounded depth.

SPECIFICATION Spec

CONSTANTS
    Users      = {alice, bob, eve}
    Sessions   = {1, 2}
    Merchants  = {M1, M2, M_attacker}
    CartHashes = {h1, h2}
    MaxNonce   = 4
    TTL        = 3

INVARIANTS
    I1_NoIdentifierLeak
    I2_AudienceBinding
    I3_CartMandateBinding
    I4_SingleUseTimeBounded
    VaultWhisperResistance
    Safety
